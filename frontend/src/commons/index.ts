import { CSSProperties } from "react";

export const override: CSSProperties = {
  position: "absolute",
  left: "50%",
};

export const sortByOptions = {
  IDLE: "IDLE",
  ASC: "ASC",
  DESC: "DESC",
};
